#include "Engine.h"  // Include the header for your Engine class

int main() {
    // Create an instance of your Engine class
    Engine engine;

    // Initialize the engine
    engine.Initialize();

    // Run the game loop
    engine.GameLoop();

    return 0;
}
